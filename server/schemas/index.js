// Import the 'typeDefs' module from the './typeDefs' file
const typeDefs = require('./typeDefs');

// Import the 'resolvers' module from the './resolvers' file
const resolvers = require('./resolvers');

// Export the 'typeDefs' and 'resolvers' objects
module.exports = { typeDefs, resolvers };